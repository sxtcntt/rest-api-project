version: '3'
services:
  web:
    build:  .
    ports:
      - "5000:80"
    volumes:
      - .:/app
    depends_on:
      - db
    env_file:
      - ./.env
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=LWrBOiiahbGufsxAUHwvDC0PjBbZVDuy
      - POSTGRES_DB=data_cbmf
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: