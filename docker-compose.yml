version: '3'
services:
  web:
    build:  .
    ports:
      - "5000:80"
    volumes:
      - .:/app
    depends_on:
      - db
    env_file:
      - ./.env
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=WNNz7sKCaPl3xFpBYkZVKPzCGqxoxV2O
      - POSTGRES_DB=data_ussl
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: